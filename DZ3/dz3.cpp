#include <stdio.h>
#include <math.h>

double is_dz3 ( double r, double a, double d )
{
    double aa = M_PI * r * r;
    double bb = sqrt (3) / 4 * a * a;
    double ss = d * d;

    return aa;
    return bb;
    return ss;
}
